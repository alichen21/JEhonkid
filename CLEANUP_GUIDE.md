# Repository 清理指南

## 📋 当前问题

你的 Repository 中包含了一些**不应该提交**的文件：

### ❌ 应该删除的文件

1. **`static/audio/`** - 65个MP3音频文件
   - 这些是运行时生成的音频文件
   - 不应该提交到 Repository
   - 每次部署时会重新生成

2. **`static/uploads/`** - 用户上传的图片
   - 这些是用户上传的临时文件
   - 不应该提交到 Repository
   - 部署时会创建空目录

3. **`Picture books/`** - 示例图片（可选）
   - 这些是测试用的示例图片
   - 可以保留，但会增加 Repository 大小
   - 建议：只保留1-2个示例图片

## ✅ 应该保留的文件

- ✅ 所有源代码文件（`.py`, `.tsx`, `.ts` 等）
- ✅ 配置文件（`Dockerfile`, `package.json`, `requirements.txt` 等）
- ✅ 文档文件（`README.md`, `docs/` 等）
- ✅ 脚本文件（`scripts/`, `deploy.sh` 等）

## 🔧 清理步骤

### 步骤1: 更新 .gitignore

我已经更新了 `.gitignore`，添加了：
```
# 运行时生成的文件
static/audio/
static/uploads/
```

### 步骤2: 从 Git 中删除这些文件（但保留本地文件）

```bash
# 从 Git 中删除 static/audio/ 和 static/uploads/
git rm -r --cached static/audio/
git rm -r --cached static/uploads/

# 可选：删除示例图片（如果不想保留）
# git rm -r --cached "Picture books/"
```

### 步骤3: 提交更改

```bash
git add .gitignore
git commit -m "Remove runtime files from repository"
git push origin main
```

## 📊 清理后的好处

1. **Repository 更小** - 减少克隆时间
2. **部署更快** - 减少需要传输的文件
3. **更清晰** - 只包含源代码和配置
4. **符合最佳实践** - 不提交运行时生成的文件

## ⚠️ 重要说明

- **删除这些文件不会影响部署**
- **部署时 Dockerfile 会创建这些目录**
- **应用运行时会自动生成这些文件**
- **本地文件不会被删除**（使用 `--cached` 参数）

## 🎯 最佳实践

### 应该提交到 Repository：
- ✅ 源代码
- ✅ 配置文件
- ✅ 文档
- ✅ 依赖清单

### 不应该提交到 Repository：
- ❌ 运行时生成的文件
- ❌ 用户上传的文件
- ❌ 构建产物（如 `node_modules/`, `.next/`）
- ❌ 环境变量文件（`.env`）
- ❌ 大文件（>100MB）

